name: Shell Commands
on: [push]
jobs:
  run-shell-command:
    runs-on: ubuntu-22.04
    steps:
      - name: echo a string
        run: eccho "some string"
      - name: multiline script
        run: |
          node -v
          npm -v
          